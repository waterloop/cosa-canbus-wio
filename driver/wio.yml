type: pkg

project:
  name: driver
  version: 0.0.1
  keywords:
  - wio
  - pkg
  compile_options:
    wio_version: 0.4.2
    default_target: tests

targets:
  tests:
    src: tests
    platform: native

dependencies:
  mcp2515-base:
    link_visibility: PUBLIC
    version: 1.0.1

